{
  "name": "NestJS Fastify",

  // Use Docker Compose for development with all services
  "dockerComposeFile": ["../docker-compose.yml", "docker-compose.yml"],
  "service": "app",
  "workspaceFolder": "/workspaces/nestjs-app",

  // Connect as node user (default in Microsoft devcontainer images)
  "remoteUser": "node"
}
